extends ../layout

block content
  include /components/breadcrumb.pug
  include /components/form.pug
  include ../shared/helpers/visual.pug

  +breadcrumb
    li
      a(href=adminIndexPagesPath())= __('admin.page.pages')
    li.is-active
      a(href="#" aria-current="page")=  __('admin.page.new')

  +form()(action=adminCreatePagesPath() method="POST")
    include _form.pug
